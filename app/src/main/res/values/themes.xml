<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mappe1s344183s303045" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="exampleToast">
        <item name="colorBackground">@color/dusgrønn</item>
        <item name="textColor">#000</item>
        <item name="iconLeft">@drawable/ikon</item>
        <item name="strokeColor">#000</item>
        <item name="strokeWidth">30dp</item>
        <item name="length">LONG</item>
        <item name="cornerRadius">3dp</item>
    </style>

</resources>